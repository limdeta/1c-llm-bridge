# ── Внутренние материалы (не для публичного репозитория) ──────────────
.internal/

# ── Локальный конфиг машины (пути к базе, пользователь) ───────────────
# Шаблон: .v8-project.json.example
.v8-project.json

# ── Среда ────────────────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc

# ── База 1С (данные, не исходники) ────────────────────────────────────
1Cv8*.1CD
*.cfl
1CHelpIndex/
1Cv8Log/
1Cv8IntegSrvc/
1Cv8JobScheduler/
srv/

# ── Временное ─────────────────────────────────────────────────────────
*.log
*.mdmp
tmp/
temp/

# ── Тяжёлые бинарники моста (self-contained ~70 МБ; собираются командой:
#    dotnet publish src/onec-cs -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o bridge/bin)
bridge/bin/

# ── IDE/OS ────────────────────────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
Desktop.ini

# ── Сборочные артефакты .NET ──────────────────────────────────────────
src/**/bin/
src/**/obj/
*.user

# ── Клон внешнего репозитория навыков (21 МБ, ставится отдельно) ──────
cc-1c-skills/
